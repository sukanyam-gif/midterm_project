# Midterm Project

I created a simple database with users, orders, and products.

I used constraints like primary keys, foreign keys, unique, and check to keep data correct.

I ran queries using joins, grouping, subqueries, and a view.

I demonstrated transactions using commit and rollback.

I also created a blocking scenario using two sessions and checked it using pg_stat_activity.

I created a role and granted permissions as part of admin tasks.
